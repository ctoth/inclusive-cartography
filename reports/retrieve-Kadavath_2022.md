# Report: Kadavath_2022

## Status
success

## Paper directory
papers/Kadavath_2022_LanguageModelsMostlyKnow/

## Commit hash
301bf53

## Retrieval notes
Primary identifier (`https://arxiv.org/abs/2207.05221`) worked via `fetch_paper.py`. The script hit Semantic Scholar rate limits (multiple HTTP 429s) during metadata resolution, which caused the background task to run for roughly 4 minutes of retry loops before arxiv-only metadata fallback completed successfully. During that retry period I also pulled the PDF directly from `https://arxiv.org/pdf/2207.05221` as insurance, then deleted that duplicate once `fetch_paper.py` finished and created the canonical `papers/Kadavath_2022_LanguageModelsMostlyKnow/` directory with `paper.pdf` and `metadata.json`. No sci-hub fallback was needed. No paywall.

## Extraction notes
- Page count: 43 pages (main body + appendices A–D + references).
- Chunked?: No — 43 ≤ 50, so direct-read per paper-reader Step 2A.
- Subagent model: None (worker read pages directly, as required for ≤50 pages).
- All 43 page images (`pngs/page-000.png` through `pngs/page-042.png`) were read individually via the Read tool.
- No unparseable sections; the bibliography on pp.42–43 was partially OCR-ambiguous, so `citations.md` notes that some references are reconstructed by author-year and topic rather than byte-for-byte verbatim.

## Files created
- notes.md: 240 lines
- metadata.json: present (auto-generated by fetch_paper.py; fields complete including arxiv_id, doi, abstract, pdf_url)
- abstract.md: present
- description.md: present
- citations.md: present
- pngs/: 43 images

## Issues / follow-ups
- **Reconcile not run.** Per the parent `prompts/retrieve-and-process-paper.md` swarm rules, I did not initiate cross-paper reconcile. A consolidation agent should run `research-papers:reconcile` on this directory after the swarm finishes.
- **papers/index.md not updated.** Per swarm rules, index entry will be added by the consolidation agent. The `description.md` body and proposed tag line (`[llm-calibration, ai-assisted-screening, self-evaluation, honesty, anthropic]`) are ready for consolidation to copy in.
- **Propstore source-branch steps skipped.** Per parent rules, `source-bootstrap`, `register-concepts`, `extract-claims`, `extract-justifications`, `extract-stances`, and `source-promote` were NOT run. Claim/concept extraction should happen downstream.
- **Citation list reconstruction.** The reference pages (42–43) were rendered at 150 dpi; some entries are paraphrased by author-year rather than fully verbatim in `citations.md`. If downstream claim extraction needs exact bibliographic formatting, consult the raw `paper.pdf` directly.
- **Critique relevance.** The paper directly supports the author-feedback critique of Wabiski 2026's fixed-threshold rule (>0.90 auto-include, 0.70–0.90 human review). See `notes.md` "Relevance to This Project" and "Limitations" — the four mechanisms (format fragility, multi-sample improvement, OOD calibration flip on GSM8k, pretraining-distribution dependence) are each citable leverage points against a blanket confidence threshold.
- **S2 rate-limit noise.** If other swarm workers are running `fetch_paper.py` in parallel they will also hit S2 429s. This is noise, not failure; arxiv-only metadata fallback still succeeds.
